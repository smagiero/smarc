# smesh is intentionally empty for now.
#
# Add Cascade targets here incrementally as the systolic array module takes
# shape. Keeping this file present lets the top-level build include the
# directory without forcing an early implementation.
